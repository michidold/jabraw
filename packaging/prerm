#!/bin/sh
set -e
if [ "$1" = "remove" ] && [ -d /run/systemd/system ]; then
    # Nur relevant, wenn sie jemand von Hand aktiviert hat.
    systemctl --global disable jabra-media-daemon.service 2>/dev/null || true
fi
exit 0
